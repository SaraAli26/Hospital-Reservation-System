@model IEnumerable<LeHospital.Models.Specialty>

@{
    ViewBag.Title = "PatientHome";
}

<h2>Receiptionist Home</h2>

<p>
    @ViewBag.userid;
</p>
<table class="table">
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.Name)*@
            Specialities
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>

            @{
                var Speciality = @Html.DisplayFor(modelItem => item.Name).ToString();
            }
            @Html.ActionLink(Speciality, "ShowClinics","Clinics", new { id = item.Id }, null)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>*@
    </tr>
}

</table>
