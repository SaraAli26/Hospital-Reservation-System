@model IEnumerable<LeHospital.Models.Appointment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.Clinic.Specialty.Name)*@
            Speciality
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Clinic.ClinicNo)
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.Clinic.Doctor.Name)*@
            Doctor Name
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Clinic.DateTime)
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.Patient.Name)*@
            Patient Name
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Clinic.Specialty.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Clinic.ClinicNo)
        </td>
        <th>
            @Html.DisplayFor(modelItem => item.Clinic.Doctor.Name)
        </th>
        <th>
            @Html.DisplayFor(modelItem => item.Clinic.DateTime)
        </th>
        <td>
            @Html.DisplayFor(modelItem => item.Patient.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
        </td>
    </tr>
}

</table>
