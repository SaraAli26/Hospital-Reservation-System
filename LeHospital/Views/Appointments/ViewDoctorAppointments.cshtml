@model IEnumerable<LeHospital.Models.Appointment>

@{
    ViewBag.Title = "View Doctor Appointments";
}

<h2>View Doctor Appointments</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Clinic.ClinicNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Clinic.DateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Clinic.AmPm)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Clinic.Time)
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.Patient.Name)*@
            Patient Name
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Clinic.ClinicNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Clinic.DateTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Clinic.AmPm)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Clinic.Time)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Patient.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
        </td>
    </tr>
}

</table>
